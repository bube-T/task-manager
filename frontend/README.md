# Taska — Frontend

React 19 + TypeScript + Vite + Tailwind CSS v4

## Setup

```bash
npm install
npm run dev
```

Requires the backend running on `http://localhost:8000`. See the root README for full setup instructions.

## Environment

Create a `.env` file:
```
VITE_API_URL=http://localhost:8000
```
